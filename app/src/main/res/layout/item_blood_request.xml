<?xml version="1.0" encoding="utf-8"?>
<!--
    Blood Request Card Item
    ┌─┬─────────────────────────────────────┐
    │▌│ [A+]  John Dela Cruz                │  ← urgency strip | blood type badge | name
    │▌│       St. Luke's Hospital • Barobo  │  ← hospital + city
    │▌│       2 units needed                │  ← units
    │▌│ ⚠ CRITICAL        [Respond] 2h ago │  ← badge + button + time
    └─┴─────────────────────────────────────┘
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_blood_request"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- ── Urgency color strip (left border) ─────────────────────────── -->
        <View
            android:id="@+id/urgency_strip"
            android:layout_width="6dp"
            android:layout_height="match_parent"
            android:background="@drawable/bg_urgency_critical"
            tools:background="@drawable/bg_urgency_critical" />

        <!-- ── Card content ──────────────────────────────────────────────── -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:padding="14dp">

            <!-- Row 1: Blood type badge + Name + Time -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- Blood Type Badge (circle) -->
                <TextView
                    android:id="@+id/tv_blood_type"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:background="@drawable/bg_blood_type_badge"
                    android:gravity="center"
                    android:text="A+"
                    android:textColor="@color/white"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    tools:text="O+" />

                <!-- Requester Name + Hospital -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="12dp"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tv_requester_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/on_surface"
                        android:textSize="15sp"
                        android:textStyle="bold"
                        android:maxLines="1"
                        android:ellipsize="end"
                        tools:text="Maria Santos" />

                    <TextView
                        android:id="@+id/tv_hospital"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="2dp"
                        android:drawablePadding="4dp"
                        android:textColor="#757575"
                        android:textSize="12sp"
                        android:maxLines="1"
                        android:ellipsize="end"
                        tools:text="📍 Surigao del Sur Provincial Hospital • Barobo" />

                </LinearLayout>

                <!-- Time posted -->
                <TextView
                    android:id="@+id/tv_time_ago"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:textColor="#BDBDBD"
                    android:textSize="11sp"
                    tools:text="2h ago" />

            </LinearLayout>

            <!-- Row 2: Description (optional) -->
            <TextView
                android:id="@+id/tv_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textColor="#616161"
                android:textSize="13sp"
                android:maxLines="2"
                android:ellipsize="end"
                android:visibility="gone"
                tools:text="Patient needs blood for emergency surgery scheduled tomorrow morning."
                tools:visibility="visible" />

            <!-- Row 3: Units needed + Urgency badge + Respond button -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- Units needed -->
                <TextView
                    android:id="@+id/tv_units"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_chip_filter"
                    android:textColor="@color/blood_red"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    tools:text="2 units" />

                <!-- Urgency label -->
                <TextView
                    android:id="@+id/tv_urgency_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    tools:text="⚠ CRITICAL"
                    tools:textColor="@color/urgency_critical" />

                <!-- Spacer -->
                <View
                    android:layout_width="0dp"
                    android:layout_height="1dp"
                    android:layout_weight="1" />

                <!-- Respond Button (Donors see this; Recipients see "View") -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_respond"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:text="Respond"
                    android:textSize="12sp"
                    app:backgroundTint="@color/blood_red"
                    android:textColor="@color/white"
                    app:cornerRadius="8dp" />

            </LinearLayout>

        </LinearLayout>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
